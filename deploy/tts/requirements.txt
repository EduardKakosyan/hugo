# faster-qwen3-tts (VEN-56): the same Qwen3-TTS CustomVoice checkpoint the
# official qwen-tts package serves, wrapped with CUDA-graph decode and
# genuine chunked streaming generation — the official package can only
# synthesize a full utterance per blocking call (its "streaming" flag
# simulates streaming text input, not generation). Requires torch>=2.5.1
# (CUDA-graph capture is broken on <=2.5.0 per its README). The `sox`
# system binary note from the qwen-tts era still applies (apt install sox).
faster-qwen3-tts
