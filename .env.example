# HUGO Personal Assistant — Environment Variables
# Copy to .env and fill in values. NEVER commit .env!

# ── Feature Flags ──────────────────────────────────────────────
HUGO_SIMULATION_MODE=true          # true = no robot hardware required
HUGO_VOICE_ENABLED=true            # Enable voice pipeline
HUGO_VISION_ENABLED=true           # Enable camera/VLM

# ── Local MLX Models ───────────────────────────────────────────
# Models are auto-downloaded on first run via MLX
MLX_LLM_MODEL=Qwen/Qwen3-32B-MLX-4bit
MLX_VLM_MODEL=Qwen/Qwen3-VL-4B-Instruct-4bit
MLX_STT_MODEL=mlx-community/whisper-large-v3-turbo
MLX_TTS_MODEL=prince-canuma/Kokoro-82M
MLX_EMBED_MODEL=nomic-ai/nomic-embed-text-v2-moe

# ── Cloud LLM (fallback) ──────────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ── MS Graph (Email, Calendar, Teams, OneDrive) ───────────────
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# ── Linear ─────────────────────────────────────────────────────
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Fireflies.ai ──────────────────────────────────────────────
FIREFLIES_API_KEY=your-fireflies-api-key

# ── Azure Infrastructure (optional) ───────────────────────────
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP=hugo-rg
COSMOSDB_CONNECTION_STRING=your-cosmosdb-connection
AZURE_STORAGE_CONNECTION_STRING=your-storage-connection

# ── Reachy Mini Robot ──────────────────────────────────────────
REACHY_HOST=localhost
REACHY_PORT=50051
